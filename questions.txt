1) Что подразумевается под Target??? в главе 4 написано что это цель которую XCode устанавливает автоматически.

2) Какой смысл добавлять фреймворк в Link Binary Если мне все-равно придется подключать его хэдр? Не проще без подключения фреймворка просто добавить хэдер. (удалил фреймворк изи Link Binary просто добавил хэдер и все работало) мм кажется понял, Это нужно лишь при использовании своих фреймворков, да?

3) В главе 4 надо утсновить делегатом locationManager делегата приложения и реализовать locationManager:didUpdateToLocation:fromLocation: метод (когда я в хэдере делегата приложения добавляю поддержку протокола  CLLocationManagerDelegate) в .m делегата приложения данный метод зачеркнут это типа устаревшая версия?


4). страница 86 метод dealloc (к кому он относится??) сам принцип понятен если делегатом locationmanager является сам кто-то? то освобождаю памятью Но я этот метод нигде не объявлял и в протоколе CLlocationManager его нет. И почему в контроллерах не обязателен метод dealoc а в моделях и view обязателен?? было написано на той же странице


5). Because you’ve used IBOutlet in these declarations, you’re going to configure the objects in a XIB file. Open MainWindow.xib and select the Window object in the outline view. This will open the UIWindow instance, and we can begin building the user interface.
Я создавал как обычно пустой проект и у меня не было MainWindow.xib пришлось создавать дополнительный xib в file's Owner указывать appDelegate потом добавлять объект указывать его делегатом приложения. Этот путь единственный?? и вообще я правильно сделал?


6). Глупый вопрос у меня документация в xcode выглядит просто (нет Navigation Bar, Search Result, Explore Option) совсем не как стр 104 это фишка новой версии XCOde нельзя вернуть старый вариант? И когда открываю target там нет Summery и что такое Capabilites?

!!!7). что такое storyboard (и scene) и где main.xib (что такое nib) ? Когда открываю например storyboard в нижней правой части есть варианты всего(file Template library; object library и так далее), что я могу создать (в кратце для чего каждый из них) и где устанавливается связь между контроллерами и view например? + еще вверху inspector

!!!8). Как все работает в редакторе (сам ход действий) по типу: 1)создаю window 2)добавляю все объекты которые используются на этом window 3). каким-то образом привязываю созданные в редакторе объекты с объектами из кода и так далее?? 